<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lunaret_seb.hb.lunaret_seb_zoo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".animaux.Listeanimaux" />
        <activity android:name=".animaux.DetailAnimal" />
        <activity android:name=".animaux.FormAddAnimaux" />
        <activity android:name=".stock.ListStockActivity" />
        <activity android:name=".stock.DetailStockActivity" />
        <activity android:name=".ticket.ListTicketActivity" />
        <activity android:name=".ticket.DetailTicketActivity" />
        <activity android:name=".enclos.ListEnclos" />
        <activity android:name=".enclos.DetailEnclos" />
        <activity android:name=".stock.FormAddStockActivity" />
        <activity
            android:name=".ticket.FormAddTicketActivity"
            android:label="@string/title_activity_form_add_ticket"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".ticket.ListTicketService"
            android:enabled="true"
            android:exported="true"></service>
    </application>

</manifest>